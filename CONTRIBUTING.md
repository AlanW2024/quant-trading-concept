# 貢獻指南（Agentic 協作最小 SOP）

本倉庫採用 **AI 協作 / 連鎖代理** 流程：以明確的角色分工與可驗證的輸出，降低風險、提升可重現性。  
**重要：本專案強制使用 `professional_multifactor_engine_pro.py`（Pro-only）**；找不到 Pro 引擎即視為錯誤。

---

## 角色分工

### 1) Planner（規劃 / 審核準則）
- 將需求拆成可驗證步驟
- 先寫「接受標準」與邊界（不准動的檔案/路徑）
- 產出：**計畫**與**命令清單**

### 2) Executor（執行 / 產出可檢查成果）
- 依計畫實作，**每一步都回報**：指令、關鍵日誌、變更摘要（或 diff 摘要）
- 碰到偏差就停下並回報差異與修正方案，不擴張範圍

### 3) Verifier（驗證 / 給結論）
- 逐條對照接受標準檢查
- 不滿足→指出證據與**最小修正**
- 全部滿足→給「通過」結論

---

## 節奏：Plan → Diff → Test → Commit → PR
1. **Plan**：寫計畫與接受標準
2. **Diff**：實作並貼出變更摘要（或檔案清單）
3. **Test**：本機跑 linters/型別/測試
4. **Commit**：語義化訊息（feat/fix/docs/chore/test…）
5. **PR**：指明關聯議題與風險/回滾方案

---

## 接受標準（範本，請複製後填入）

```text
目標：<一句話說明>
必須滿足：
- 退出碼：關鍵命令皆為 0
- 產物/檔案：列出新增/修改/刪除的精準路徑
- 測試：pytest 指定測試名稱通過；若缺 Pro，引導 skip 而非 fail
- 檢查：black --check、ruff、mypy 全數通過（非警告）
- 邊界：不得修改 <paths>；不得移除 <files>
風險與回滾：
- 風險：<例如清理快取 / 重寫檔案>
- 回滾：`git revert <commit>` 或移除 <檔案> 並重跑測試
```
